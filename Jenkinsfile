pipeline {
	agent {
label {
	 	label "built-in"
		customWorkspace  '/mnt/pipe'
}
}
	stages {
		stage ('stage-1') {
			steps  {
				sh 'mkdir abc'
}
}
		stages {
		stage ('stage-2') {
			steps  {
				sh 'mkdir zxc'
}
}
}
}
